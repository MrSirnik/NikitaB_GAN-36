using UnityEngine;

namespace FPS
{
    // Метка на сцене: уровень уже собран заранее в редакторе, GameBootstrap.Run()
    // не должен пересобирать его с нуля при каждом запуске Play.
    public class BakedLevelMarker : MonoBehaviour
    {
    }
}
